# URL do backend sem o sufixo /api. O Next.js encaminha /api/* para esta origem.
BACKEND_URL=http://localhost:8080

# Mantém as chamadas do navegador no mesmo domínio do frontend.
NEXT_PUBLIC_API_URL=/api
